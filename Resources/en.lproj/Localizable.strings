/*
  Localizable.strings (English)
  Bloom
*/

"健康度" = "健康度";
"用 Bloom 养成喝水好习惯" = "用 Bloom 养成喝水好习惯";
"成就解锁！" = "成就解锁！";
"data.plantSymbol)
                    .font(.system(size: 120))
                
                // 植物信息
                VStack(spacing: 16) {
                    Text(data.plantName)
                        .font(.system(size: 56, weight: .bold))
                        .foregroundColor(.white)
                    
                    Text(data.plantStage)
                        .font(.system(size: 32))
                        .foregroundColor(.white.opacity(0.9))
                }
                
                // 健康度
                VStack(spacing: 12" = "data.plantSymbol)
                    .font(.system(size: 120))
                
                // 植物信息
                VStack(spacing: 16) {
                    Text(data.plantName)
                        .font(.system(size: 56, weight: .bold))
                        .foregroundColor(.white)
                    
                    Text(data.plantStage)
                        .font(.system(size: 32))
                        .foregroundColor(.white.opacity(0.9))
                }
                
                // 健康度
                VStack(spacing: 12";
"植物健康" = "植物健康";
"状态" = "状态";
"更新于 \(data.lastUpdated, style: .time)" = "更新于 \(data.lastUpdated, style: .time)";
"data.lastUpdated, style: .time)
                        .font(.system(size: 10))
                        .foregroundColor(.secondary)
                }
            }
            
            Spacer()
            
            // 右侧：进度环
            ZStack {
                Circle()
                    .stroke(Color.gray.opacity(0.2), lineWidth: 8)
                
                Circle()
                    .trim(from: 0, to: data.progressPercentage)
                    .stroke(
                        LinearGradient(
                            colors: [Color.bloom, Color.bloomPrimary],
                            startPoint: .topLeading,
                            endPoint: .bottomTrailing
                        ),
                        style: StrokeStyle(lineWidth: 8, lineCap: .round)
                    )
                    .rotationEffect(.degrees(-90))
                
                VStack(spacing: 2" = "data.lastUpdated, style: .time)
                        .font(.system(size: 10))
                        .foregroundColor(.secondary)
                }
            }
            
            Spacer()
            
            // 右侧：进度环
            ZStack {
                Circle()
                    .stroke(Color.gray.opacity(0.2), lineWidth: 8)
                
                Circle()
                    .trim(from: 0, to: data.progressPercentage)
                    .stroke(
                        LinearGradient(
                            colors: [Color.bloom, Color.bloomPrimary],
                            startPoint: .topLeading,
                            endPoint: .bottomTrailing
                        ),
                        style: StrokeStyle(lineWidth: 8, lineCap: .round)
                    )
                    .rotationEffect(.degrees(-90))
                
                VStack(spacing: 2";
"喝水趋势" = "喝水趋势";
"最佳喝水时间" = "最佳喝水时间";
"上午 9-10 点是你喝水最频繁的时段，继续保持！" = "上午 9-10 点是你喝水最频繁的时段，继续保持！";
"每周习惯" = "每周习惯";
"周三、周五是你的喝水高峰日" = "周三、周五是你的喝水高峰日";
"成就进度" = "成就进度";
"已解锁" = "已解锁";
"高级统计" = "高级统计";
"range.rawValue).tag(range)
            }
        }
        .pickerStyle(.segmented)
    }
    
    // MARK: - 核心统计卡片
    
    private var statsOverview: some View {
        let stats = calculateStats()
        
        return LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 12" = "range.rawValue).tag(range)
            }
        }
        .pickerStyle(.segmented)
    }
    
    // MARK: - 核心统计卡片
    
    private var statsOverview: some View {
        let stats = calculateStats()
        
        return LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible())], spacing: 12";
"成就" = "成就";
"数据已成功恢复" = "数据已成功恢复";
"请在系统设置中允许 Bloom 访问健康数据" = "请在系统设置中允许 Bloom 访问健康数据";
"请在系统设置中允许 Bloom 发送通知" = "请在系统设置中允许 Bloom 发送通知";
"暂停期间植物不会枯萎，最长可暂停14天。出差/旅行时非常有用。" = "暂停期间植物不会枯萎，最长可暂停14天。出差/旅行时非常有用。";
"确定要恢复养护吗？植物将重新开始生长。" = "确定要恢复养护吗？植物将重新开始生长。";
"植物名字" = "植物名字";
"品种" = "品种";
"恢复养护" = "Resume Care";
"剩余 (plantEngine.plant.remainingPauseDays) 天" = "剩余 (plantEngine.plant.remainingPauseDays) 天";
"暂停养护" = "Pause Care";
"出差/旅行" = "出差/旅行";
"我的植物" = "我的植物";
"每日目标" = "每日目标";
"建议成人每日饮水 2000ml" = "建议成人每日饮水 2000ml";
"每 \($0) 分钟" = "每 \($0) 分钟";
"提醒" = "提醒";
"开启后，植物口渴时会提醒你来浇水" = "开启后，植物口渴时会提醒你来浇水";
"连接健康 App" = "连接健康 App";
"健康 App" = "健康 App";
"主题颜色" = "主题颜色";
"外观" = "外观";
"iCloud 同步" = "iCloud 同步";
"未登录" = "未登录";
"自动同步到 iCloud，多设备数据保持一致" = "自动同步到 iCloud，多设备数据保持一致";
"导出数据备份" = "导出数据备份";
"从备份恢复" = "从备份恢复";
"上次备份" = "上次备份";
"数据备份" = "数据备份";
"导出 JSON 文件可保存到 Files App，用于数据备份或迁移" = "导出 JSON 文件可保存到 Files App，用于数据备份或迁移";
"Bloom Pro 已解锁" = "Bloom Pro 已解锁";
"升级 Bloom Pro" = "升级 Bloom Pro";
"解锁更多品种" = "解锁更多品种";
"版本" = "版本";
"关于" = "关于";
"去设置" = "去设置";
"恢复" = "恢复";
"恢复购买" = "恢复购买";
"设置" = "Settings";
"恢复成功" = "恢复成功";
"恢复失败" = "恢复失败";
"健康权限" = "健康权限";
"通知权限" = "通知权限";
"plantEngine.plant.species.name)
                    .foregroundStyle(.secondary)
                Text(plantEngine.plant.species.symbol)
            }

            // 暂停/恢复养护
            if plantEngine.plant.isPaused {
                Button(role: .destructive" = "plantEngine.plant.species.name)
                    .foregroundStyle(.secondary)
                Text(plantEngine.plant.species.symbol)
            }

            // 暂停/恢复养护
            if plantEngine.plant.isPaused {
                Button(role: .destructive";
"选择你喜欢的主题外观" = "选择你喜欢的主题外观";
"主题外观" = "主题外观";
"正在养护" = "正在养护";
"收获墙" = "收获墙";
"养护 \(item.daysToHarvest) 天" = "养护 \(item.daysToHarvest) 天";
"品种图鉴" = "品种图鉴";
"未收集" = "未收集";
"我的花园" = "My Garden";
"icon).font(.system(size: 20))
            Text(value).font(.system(size: 20, weight: .bold, design: .rounded))
            Text(label).font(.system(size: 11)).foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity)
    }

    // MARK: - 当前植物

    private var currentPlantCard: some View {
        VStack(spacing: 12" = "icon).font(.system(size: 20))
            Text(value).font(.system(size: 20, weight: .bold, design: .rounded))
            Text(label).font(.system(size: 11)).foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity)
    }

    // MARK: - 当前植物

    private var currentPlantCard: some View {
        VStack(spacing: 12";
"太棒了！" = "太棒了！";
"achievement.title)
                    .font(.system(size: 14, weight: .semibold))
                    .lineLimit(1)
                
                Text(achievement.description)
                    .font(.system(size: 11))
                    .foregroundColor(.secondary)
                    .lineLimit(2)
            }
            
            // 进度条
            if !achievement.isUnlocked {
                ProgressView(value: achievement.progressPercentage)
                    .tint(.bloomPrimary" = "achievement.title)
                    .font(.system(size: 14, weight: .semibold))
                    .lineLimit(1)
                
                Text(achievement.description)
                    .font(.system(size: 11))
                    .foregroundColor(.secondary)
                    .lineLimit(2)
            }
            
            // 进度条
            if !achievement.isUnlocked {
                ProgressView(value: achievement.progressPercentage)
                    .tint(.bloomPrimary";
"连续 \(streak) 天达标" = "连续 \(streak) 天达标";
"再坚持 \(next.days - streak) 天 → \(next.emoji) \(next.title)" = "再坚持 \(next.days - streak) 天 → \(next.emoji) \(next.title)";
"下一阶段：\(next)" = "下一阶段：\(next)";
"可以收获了！" = "可以收获了！";
"HealthCalculator.statusMessage(plantEngine.plant.health))
                .font(.system(size: 15, weight: .medium))
                .foregroundStyle(healthColor)
                .frame(maxWidth: .infinity, alignment: .leading)

            // 健康度进度条
            healthBar

            // 成长进度（距离下一阶段）
            growthProgressBar

            // 连胜 + 里程碑
            if waterStore.currentStreak > 0 {
                streakRow
            }
        }
        .padding(18)
        .background(Color(.secondarySystemBackground))
        .clipShape(RoundedRectangle(cornerRadius: 20, style: .continuous))
    }

    // MARK: - 子视图

    private var stageBadge: some View {
        Text(plantEngine.plant.stage.emoji)
            .font(.system(size: 24))
            .padding(8)
            .background(Color.bloomPrimary.opacity(0.12))
            .clipShape(Circle())
    }

    private var healthBar: some View {
        VStack(alignment: .leading, spacing: 4" = "HealthCalculator.statusMessage(plantEngine.plant.health))
                .font(.system(size: 15, weight: .medium))
                .foregroundStyle(healthColor)
                .frame(maxWidth: .infinity, alignment: .leading)

            // 健康度进度条
            healthBar

            // 成长进度（距离下一阶段）
            growthProgressBar

            // 连胜 + 里程碑
            if waterStore.currentStreak > 0 {
                streakRow
            }
        }
        .padding(18)
        .background(Color(.secondarySystemBackground))
        .clipShape(RoundedRectangle(cornerRadius: 20, style: .continuous))
    }

    // MARK: - 子视图

    private var stageBadge: some View {
        Text(plantEngine.plant.stage.emoji)
            .font(.system(size: 24))
            .padding(8)
            .background(Color.bloomPrimary.opacity(0.12))
            .clipShape(Circle())
    }

    private var healthBar: some View {
        VStack(alignment: .leading, spacing: 4";
"免费用户最多保存 \(GardenStore.freeUserGardenLimit) 株植物。升级 Pro 解锁无限花园！" = "免费用户最多保存 \(GardenStore.freeUserGardenLimit) 株植物。升级 Pro 解锁无限花园！";
"暂停养护中" = "暂停养护中";
"剩余 \(plantEngine.plant.remainingPauseDays) 天" = "剩余 \(plantEngine.plant.remainingPauseDays) 天";
"收获 \(plantEngine.plant.name)" = "收获 \(plantEngine.plant.name)";
"今日记录" = "今日记录";
"\(plantEngine.plant.name) 还没喝到水" = "\(plantEngine.plant.name) 还没喝到水";
"长大啦！" = "长大啦！";
"进入了「\(stage.name)」阶段" = "进入了「\(stage.name)」阶段";
"继续守护" = "继续守护";
"升级 Pro" = "Upgrade to Pro";
"花园已满" = "Garden Full";
"record.timeString)
                .font(.system(size: 13))
                .foregroundStyle(.secondary)
            Spacer()
            Text(record.formattedAmount)
                .font(.system(size: 14, weight: .semibold, design: .rounded))
                .foregroundStyle(Color.bloomWater)
        }
        .padding(.vertical, 8)
    }
}

// MARK: - 阶段升级庆祝

struct StageUpCelebration: View {
    let stage: GrowthStage
    let onDismiss: () -> Void

    @State private var appear = false

    var body: some View {
        ZStack {
            // 半透明遮罩
            Color.black.opacity(0.45)
                .ignoresSafeArea()
                .onTapGesture { onDismiss() }

            VStack(spacing: 18) {
                // emoji + 光环
                ZStack {
                    Circle()
                        .fill(
                            RadialGradient(
                                colors: [Color.bloomGold.opacity(0.5), .clear],
                                center: .center,
                                startRadius: 10,
                                endRadius: 70
                            )
                        )
                        .frame(width: 140, height: 140)
                        .scaleEffect(appear ? 1.0 : 0.4)

                    Text(stage.emoji)
                        .font(.system(size: 76))
                        .scaleEffect(appear ? 1.0 : 0.3" = "record.timeString)
                .font(.system(size: 13))
                .foregroundStyle(.secondary)
            Spacer()
            Text(record.formattedAmount)
                .font(.system(size: 14, weight: .semibold, design: .rounded))
                .foregroundStyle(Color.bloomWater)
        }
        .padding(.vertical, 8)
    }
}

// MARK: - 阶段升级庆祝

struct StageUpCelebration: View {
    let stage: GrowthStage
    let onDismiss: () -> Void

    @State private var appear = false

    var body: some View {
        ZStack {
            // 半透明遮罩
            Color.black.opacity(0.45)
                .ignoresSafeArea()
                .onTapGesture { onDismiss() }

            VStack(spacing: 18) {
                // emoji + 光环
                ZStack {
                    Circle()
                        .fill(
                            RadialGradient(
                                colors: [Color.bloomGold.opacity(0.5), .clear],
                                center: .center,
                                startRadius: 10,
                                endRadius: 70
                            )
                        )
                        .frame(width: 140, height: 140)
                        .scaleEffect(appear ? 1.0 : 0.4)

                    Text(stage.emoji)
                        .font(.system(size: 76))
                        .scaleEffect(appear ? 1.0 : 0.3";
"深度洞察" = "深度洞察";
"植物成长历程" = "植物成长历程";
"\(plantEngine.plant.species.name) · \(plantEngine.plant.stage.name) · 种植 \(plantEngine.plant.ageInDays) 天" = "\(plantEngine.plant.species.name) · \(plantEngine.plant.stage.name) · 种植 \(plantEngine.plant.ageInDays) 天";
"深度数据洞察" = "深度数据洞察";
"解锁达标率分析、平均完成度、成长历程等深度数据，更科学地养成喝水习惯。" = "解锁达标率分析、平均完成度、成长历程等深度数据，更科学地养成喝水习惯。";
"解锁 Bloom Pro" = "Unlock Bloom Pro";
"喝水记录" = "Water Log";
"$0.rawValue).tag($0) }
                    }
                    .pickerStyle(.segmented)

                    chartView
                        .frame(height: 200)
                }
                .padding(16)
                .background(Color(.secondarySystemBackground))
                .clipShape(RoundedRectangle(cornerRadius: 20, style: .continuous))
                .padding(.horizontal, 20)

                // Pro 深度洞察（免费用户看到锁定卡片）
                proInsightCard
                    .padding(.horizontal, 20)

                Spacer(minLength: 80)
            }
            .padding(.top, 4)
        }
        .scrollIndicators(.hidden)
        .background(Color(.systemGroupedBackground)" = "$0.rawValue).tag($0) }
                    }
                    .pickerStyle(.segmented)

                    chartView
                        .frame(height: 200)
                }
                .padding(16)
                .background(Color(.secondarySystemBackground))
                .clipShape(RoundedRectangle(cornerRadius: 20, style: .continuous))
                .padding(.horizontal, 20)

                // Pro 深度洞察（免费用户看到锁定卡片）
                proInsightCard
                    .padding(.horizontal, 20)

                Spacer(minLength: 80)
            }
            .padding(.top, 4)
        }
        .scrollIndicators(.hidden)
        .background(Color(.systemGroupedBackground)";
"value).font(.system(size: 20, weight: .bold, design: .rounded))
            }
            Text(label).font(.system(size: 11)).foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity)
    }

    // MARK: - Pro 深度洞察卡

    private var proInsightCard: some View {
        Group {
            if isPro {
                VStack(alignment: .leading, spacing: 14" = "value).font(.system(size: 20, weight: .bold, design: .rounded))
            }
            Text(label).font(.system(size: 11)).foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity)
    }

    // MARK: - Pro 深度洞察卡

    private var proInsightCard: some View {
        Group {
            if isPro {
                VStack(alignment: .leading, spacing: 14";
"label).font(.system(size: 13)).foregroundStyle(.secondary)
                Text(detail).font(.system(size: 11)).foregroundStyle(.tertiary)
            }
            Spacer()
            Text(value)
                .font(.system(size: 20, weight: .bold, design: .rounded))
                .foregroundStyle(color)
        }
    }

    // MARK: - 统计计算

    private var periodData: [(date: Date, amount: Int)] {
        let cal = Calendar.current
        let end = Date()
        let start = cal.date(byAdding: .day, value: -(period.days - 1), to: end) ?? Date()
        return waterStore.dailyTotals(from: start, to: end)
    }

    private var totalDays: Int { periodData.count }

    private var achievedDays: Int {
        periodData.filter { $0.amount >= userStore.dailyGoal }.count
    }

    private var achievementRate: Int {
        guard totalDays > 0 else { return 0 }
        return Int(Double(achievedDays) / Double(totalDays) * 100)
    }

    private var avgDailyMl: Int {
        guard totalDays > 0 else { return 0 }
        return periodData.reduce(0) { $0 + $1.amount } / totalDays
    }

    private var avgCompletionPercent: Int {
        guard userStore.dailyGoal > 0 else { return 0 }
        return min(Int(Double(avgDailyMl) / Double(userStore.dailyGoal) * 100), 999)
    }

    private var bestDayAmount: Int {
        periodData.map(\.amount).max() ?? 0
    }

    private var bestDayLabel: String {
        guard let best = periodData.max(by: { $0.amount < $1.amount }" = "label).font(.system(size: 13)).foregroundStyle(.secondary)
                Text(detail).font(.system(size: 11)).foregroundStyle(.tertiary)
            }
            Spacer()
            Text(value)
                .font(.system(size: 20, weight: .bold, design: .rounded))
                .foregroundStyle(color)
        }
    }

    // MARK: - 统计计算

    private var periodData: [(date: Date, amount: Int)] {
        let cal = Calendar.current
        let end = Date()
        let start = cal.date(byAdding: .day, value: -(period.days - 1), to: end) ?? Date()
        return waterStore.dailyTotals(from: start, to: end)
    }

    private var totalDays: Int { periodData.count }

    private var achievedDays: Int {
        periodData.filter { $0.amount >= userStore.dailyGoal }.count
    }

    private var achievementRate: Int {
        guard totalDays > 0 else { return 0 }
        return Int(Double(achievedDays) / Double(totalDays) * 100)
    }

    private var avgDailyMl: Int {
        guard totalDays > 0 else { return 0 }
        return periodData.reduce(0) { $0 + $1.amount } / totalDays
    }

    private var avgCompletionPercent: Int {
        guard userStore.dailyGoal > 0 else { return 0 }
        return min(Int(Double(avgDailyMl) / Double(userStore.dailyGoal) * 100), 999)
    }

    private var bestDayAmount: Int {
        periodData.map(\.amount).max() ?? 0
    }

    private var bestDayLabel: String {
        guard let best = periodData.max(by: { $0.amount < $1.amount }";
"⚠️ 沙盒测试模式" = "⚠️ 沙盒测试模式";
"🧪 开发模式" = "🧪 开发模式";
"请在 App Store Connect 配置商品后测试" = "请在 App Store Connect 配置商品后测试";
"或在 Scheme 中添加 BLOOM_DEV_MODE=1 环境变量" = "或在 Scheme 中添加 BLOOM_DEV_MODE=1 环境变量";
"暂未配置内购商品" = "IAP Products Not Configured";
"解锁更多生命" = "解锁更多生命";
"养出更美的花园，收集全部品种" = "养出更美的花园，收集全部品种";
"订阅自动续期，可随时在系统设置中取消" = "订阅自动续期，可随时在系统设置中取消";
"最划算" = "最划算";
"关闭" = "关闭";
"product.displayPrice)
                    .font(.system(size: 18, weight: .bold, design: .rounded))
            }
            .padding(16)
            .background(isSelected ? Color.bloomPrimary.opacity(0.1) : Color(.secondarySystemBackground))
            .overlay(
                RoundedRectangle(cornerRadius: 16)
                    .stroke(isSelected ? Color.bloomPrimary : Color.clear, lineWidth: 2)
            )
            .clipShape(RoundedRectangle(cornerRadius: 16, style: .continuous))
        }
        .buttonStyle(.plain)
        .disabled(storeManager.isPurchasing)
        .overlay {
            if storeManager.isPurchasing && isSelected {
                ProgressView()
            }
        }
    }

    private func purchase(_ product: Product) async {
        await storeManager.purchase(product)
        // 购买成功后关闭
        if storeManager.isPro {
            Haptics.success()
            dismiss()
        }
    }
}

#Preview {
    PaywallView()
        .environmentObject(StoreManager.shared" = "product.displayPrice)
                    .font(.system(size: 18, weight: .bold, design: .rounded))
            }
            .padding(16)
            .background(isSelected ? Color.bloomPrimary.opacity(0.1) : Color(.secondarySystemBackground))
            .overlay(
                RoundedRectangle(cornerRadius: 16)
                    .stroke(isSelected ? Color.bloomPrimary : Color.clear, lineWidth: 2)
            )
            .clipShape(RoundedRectangle(cornerRadius: 16, style: .continuous))
        }
        .buttonStyle(.plain)
        .disabled(storeManager.isPurchasing)
        .overlay {
            if storeManager.isPurchasing && isSelected {
                ProgressView()
            }
        }
    }

    private func purchase(_ product: Product) async {
        await storeManager.purchase(product)
        // 购买成功后关闭
        if storeManager.isPro {
            Haptics.success()
            dismiss()
        }
    }
}

#Preview {
    PaywallView()
        .environmentObject(StoreManager.shared";
"养活一株植物" = "Keep a Plant Alive";
"只需要你好好喝水" = "Just by Drinking Water Regularly";
"你每喝一口水，它就长大一点" = "It grows with every sip you drink";
"认识一下你的植物" = "Meet Your Plant";
"给它起个名字" = "Give it a name";
"每日喝水目标" = "Daily Water Goal";
"ml / 天 · 建议成人每日 2000ml" = "ml/day · Recommended 2000ml for adults";
"让 \(plantName) 在渴的时候" = "Let \(plantName) when it's thirsty";
"能告诉你" = "Can tell you";
"开启通知，它口渴时会提醒你来浇水。\n不开启也可以，但你可能会忘了它。" = "开启通知，它口渴时会提醒你来浇水。\n不开启也可以，但你可能会忘了它。";
"暂不开启" = "Not Now";
"title)
                .font(.system(size: 17, weight: .semibold))
                .foregroundStyle(.white)
                .frame(maxWidth: .infinity)
                .padding(.vertical, 16)
                .background(Color.bloomPrimary)
                .clipShape(RoundedRectangle(cornerRadius: 14, style: .continuous))
        }
    }
}

// MARK: - 触觉反馈工具

enum Haptics {
    static func light() {
        UIImpactFeedbackGenerator(style: .light).impactOccurred()
    }
    static func medium() {
        UIImpactFeedbackGenerator(style: .medium).impactOccurred()
    }
    static func success() {
        UINotificationFeedbackGenerator().notificationOccurred(.success)
    }
}

#Preview {
    OnboardingView()
        .environmentObject(UserStore())
        .environmentObject(PlantEngine())
        .environmentObject(NotificationManager.shared" = "title)
                .font(.system(size: 17, weight: .semibold))
                .foregroundStyle(.white)
                .frame(maxWidth: .infinity)
                .padding(.vertical, 16)
                .background(Color.bloomPrimary)
                .clipShape(RoundedRectangle(cornerRadius: 14, style: .continuous))
        }
    }
}

// MARK: - 触觉反馈工具

enum Haptics {
    static func light() {
        UIImpactFeedbackGenerator(style: .light).impactOccurred()
    }
    static func medium() {
        UIImpactFeedbackGenerator(style: .medium).impactOccurred()
    }
    static func success() {
        UINotificationFeedbackGenerator().notificationOccurred(.success)
    }
}

#Preview {
    OnboardingView()
        .environmentObject(UserStore())
        .environmentObject(PlantEngine())
        .environmentObject(NotificationManager.shared";

// Missing strings added during final audit
"花园限制提示 %d 株植物" = "Free users can save up to %d plants. Upgrade to Pro for unlimited garden!";
"剩余 %d 天" = "%d days remaining";
"恢复购买成功" = "Restore Successful";
"恢复购买失败" = "Restore Failed";
"未找到已购买的记录" = "No previous purchases found";
"感谢您的支持！Pro 权益已解锁。" = "Thank you for your support! Pro features unlocked.";
"无限植物品种" = "Unlimited Plant Species";
"解锁全部7种植物，打造梦幻花园" = "Unlock all 7 plants and build your dream garden";
"正在加载..." = "Loading...";
